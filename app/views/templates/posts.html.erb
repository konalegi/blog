

  <div class="well row">


      <div class="blog-post" ng-repeat="post in allPosts">
        <h2 class="blog-post-title"><a href="" ng-click="viewPost(post.id)">{{post.title}}</a> </h2>
        <p class="blog-post-meta">{{post.created_at}} by <a href="">{{post.user_name}}</a></p>

        {{post.text_data}}
        <div ng-show="{{post.signed}}"><a href="" ng-click="deletePost(post.id,$index)">Delete</a></div>
        <div ng-show="{{post.signed}}"><a href="" ng-click="editPost(post.id)">Edit</a></div>
      </div>
      <!--<ul class="pager">-->
        <!--<li><a href="#">Previous</a></li>-->
        <!--<li><a href="#">Next</a></li>-->
      <!--</ul>-->

  </div>





